[package]
name = "catalog-normalize"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Shared catalog normalization: match keys, artist names, SoundCloud title parsing"

[dependencies]
once_cell = "1"
regex = "1"
serde = { version = "1", features = ["derive"] }
unicode-normalization = "0.1"
