[package]
name = "redact"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A value that never prints itself, and url redaction for connection strings"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
