# torch / torchaudio устанавливаются отдельно в каждом Dockerfile — версия и
# CUDA-индекс зависят от целевой ветки (CUDA 13 vs CUDA 12.8 LTS / Volta-support).
# Не дублируем здесь, иначе `pip install -r` пытался бы апгрейдить wheel поверх
# Dockerfile-овского пина.

nats-py>=2.10.0
transformers>=5.0.0
accelerate>=1.0.0
aiohttp>=3.13.0
numpy>=1.26.0,<2.0
sentence-transformers>=4.0.0
huggingface-hub>=0.36.0
faster-whisper>=1.2.0
muq==0.1.0
demucs==4.0.1
gensim>=4.3.3
lightgbm>=4.6.0
