export DATABASE_URL="${DATABASE_URL:-postgres://soundcloud:devpw@127.0.0.1:55433/soundcloud_desktop}"
export OPS_DATABASE_URL="${OPS_DATABASE_URL:-$DATABASE_URL}"
export DATABASE_REQUIRE_MTLS="${DATABASE_REQUIRE_MTLS:-false}"
export OPS_DATABASE_REQUIRE_MTLS="${OPS_DATABASE_REQUIRE_MTLS:-false}"
export REDIS_URL="${REDIS_URL:-redis://127.0.0.1:6379}"
export NATS_URL="${NATS_URL:-nats://127.0.0.1:4224}"
export QDRANT_URL="${QDRANT_URL:-http://127.0.0.1:6334}"
export STREAMING_SERVICE_URL="${STREAMING_SERVICE_URL:-http://127.0.0.1:8080}"
export INTERNAL_TOKEN="${INTERNAL_TOKEN:-local-internal}"
export STORAGE_URL="${STORAGE_URL:-http://127.0.0.1:9000}"
export STORAGE_TOKEN="${STORAGE_TOKEN:-local-storage}"
export STREAM_TICKET_KEY="${STREAM_TICKET_KEY:-$(head -c 32 /dev/urandom | base64 -w0)}"
